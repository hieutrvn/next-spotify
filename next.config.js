/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "ifcetplgmdfctrfcodtr.supabase.co",
      },
    ],
  },
};

module.exports = nextConfig;
